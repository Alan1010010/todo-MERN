const express = require('express');
const router = express.Router();


router.get('/todos', (res, req, next) => {

});

router.post('/todos', (res, req, next) => {

});

router.delete('todos:id', (res, req, next) => {

});

module.exports = router;
